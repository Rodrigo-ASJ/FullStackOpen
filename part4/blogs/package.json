{
  "name": "blogs",
  "version": "1.0.0",
  "description": "app blog listing ranking",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "logs:prod": "fly logs",
    "deploy": "fly deploy",
    "testonly": "cross-env NODE_ENV=test node --test --test-only",
    "test": "cross-env NODE_ENV=test node --test"
  },
  "author": "rodrigo",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "0.7.10",
    "cross-env": "10.0.0",
    "nodemon": "3.1.10",
    "supertest": "7.1.4"
  },
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "17.2.2",
    "express": "5.1.0",
    "lodash": "4.17.21",
    "mongoose": "8.18.0"
  }
}
